# 代码审查报告 · 未实现功能与遗留问题

> 版本: 1.0.0 | 日期: 2026-07-08 | 审查范围: 全部非测试 Go 源码 (145+ 文件)
>
> 三个并行 `explore` 子智能体分别审查：协议层、网络层、游戏层。
> 共发现 **~100 项问题**，筛选合并后保留 **60 项可操作发现**。

---

## 一、🔴 生产代码关键问题 (非 Cosmos 桩)

### 1.1 运行时风险 (3项)

| # | 文件:行 | 问题 | 影响 |
|:---|:---|:---|:---|
| R01 | `app/chain.go:59,74,81-82` | `BeginBlocker(nil)` / `EndBlocker(nil)` / `GetAllUsers(nil)` 传 nil context。若内部 keeper 尝试使用 context 将 **panic** | 运行时崩溃 |
| R02 | `x/vrg/store.go:45-47` | `NewVRGStore` 中 `_ = err` 静默忽略 bucket 创建错误，store 可能损坏但不报错 | 静默数据损坏 |
| R03 | `x/vrg/store.go:85` | `LoadGenesisTree` 中 `stringNodes == nil` 后继续迭代 nil map → **panic** | 启动时崩溃 |

### 1.2 ZK 电路赋值硬编码零值 (3项)

| # | 文件:行 | 问题 | 影响 |
|:---|:---|:---|:---|
| Z01 | `x/zk/verifier.go:153-154` | `NewIdentityAssignment` 中 `MerkleRoot: 0, TrustThreshold: 0`，注释"生产环境从链上获取"但从未实现 | 身份证明验证无意义 |
| Z02 | `x/zk/verifier.go:182-183` | `NewCreditAssignment` 中 `CreditTreeRoot: 0`，同上 | 信用证明验证无意义 |
| Z03 | `x/zk/verifier.go:219` | `NewWorkAssignment` 中 `Epoch: 0`，同上 | 工作量证明 Epoch 绑定失效 |

### 1.3 数据正确性 (3项)

| # | 文件:行 | 问题 | 影响 |
|:---|:---|:---|:---|
| D01 | `x/vrg/shadow_receipt.go:244` | 收据 ID 使用 `time.Now().UnixNano()` 生成，跨节点不可复现 | 收据 ID 不一致 |
| D02 | `x/vrg/epoch.go:224` | `CollisionEvent.SourceCommunityB` 从未被填充（始终为零值） | 冲突广播不完整 |
| D03 | `app/app.go:327-343` | `recordNodePerformance` 乐观计数所有节点在所有区块中，无实际在线检测 | 在线率指标虚假 |

### 1.4 网络层缺陷 (3项)

| # | 文件:行 | 问题 | 影响 |
|:---|:---|:---|:---|
| N01 | `node/topology.go:48-59` | `AddCoreFriend` 从未设置 `IPv6`/`Port` 字段 | 好友连接失败 |
| N02 | `node/node.go:377-403` | `buildBeaconPacket` 从未设置 `params.SenderIPv6`，beacon 中 IPv6 为零值 | 信标中无有效地址 |
| N03 | `noise/handshake.go:570-591` | `isLowOrderPoint` 仅检查 1/2 阶点，Curve25519 的 4/8 阶点未检查 | 低阶点攻击面 |

### 1.5 API 硬编码 (2项)

| # | 文件:行 | 问题 | 影响 |
|:---|:---|:---|:---|
| A01 | `localapi/grpc_server.go:329` | `Height: 0` 硬编码，注释"由后续区块填充" | API 返回假数据 |
| A02 | `localapi/grpc_server.go:360` | `PoolBalance: 0` 硬编码，注释"生产环境从链上查询" | API 返回假数据 |

### 1.6 共识层 (1项)

| # | 文件:行 | 问题 | 影响 |
|:---|:---|:---|:---|
| C01 | `consensus/consensus.go:107-111` | `ProposeBlock` 始终返回 nil block data | 空区块 |

---

## 二、🟠 游戏层未实现与缺陷

### 2.1 架构集成缺陷 (2项)

| # | 文件 | 问题 |
|:---|:---|:---|
| G01 | `app/game/` 全部 8 文件 | **零测试覆盖** — 无任何 `_test.go` 文件 |
| G02 | `app/game/` → `app/app.go` | 游戏层完全未连接到应用 — `app.go` 使用 `brighttypes.GameInfo` 而非 `app/game/` 中的类型，游戏层是**死代码孤岛** |

### 2.2 引用但未实现的函数/系统 (4项)

| # | 文件:行 | 问题 |
|:---|:---|:---|
| G03 | `social/guild.go:151,206` | `TransferLeadership` 被错误消息引用但**函数不存在** — 会长无法转让或退出 |
| G04 | `social/guild.go:26-43` | `GuildSkill` 系统完整定义但**无任何操作方法**（无 add/levelup/apply） |
| G05 | `state/machine.go:21,46-51` | `ActivityState` 完整定义但**无任何代码创建活动** — 活动系统是空壳 |
| G06 | `state/machine.go:23,53-61` | `MarketState`/`MarketListing` 完整定义但**无任何操作方法** — 市场系统是空壳 |

### 2.3 功能缺陷 (4项)

| # | 文件:行 | 问题 |
|:---|:---|:---|
| G07 | `loot/algorithm.go:168-178` | `rollFloat` 混合 `crypto/rand`(非确定性) + 伪随机种子 → **掉落结果不可复现**，区块链致命缺陷 |
| G08 | `author/governance.go:226-263` | `ProcessExpiredChanges` 记录审计日志但**从未将新参数应用到 economy** |
| G09 | `social/guild.go:572-591` | `AddXP` 无返回值，silently 忽略不存在的 guild |
| G10 | `social/party.go:158-170` | `GetPartyKillContributions` 返回后**清零 map** — 破坏性读取，调用两次丢失数据 |

### 2.4 缺失验证 (4项)

| # | 文件:行 | 问题 |
|:---|:---|:---|
| G11 | `asset/asset.go:207-221` | `ValidateAsset` 不验证 Metadata/History/Name/Type |
| G12 | `social/guild.go:70-111` | `CreateGuild` 无名称长度/重复/字符集验证 |
| G13 | `social/party.go:60-86` | `InviteToParty` 无接受/拒绝流程，强制加入 |
| G14 | `asset/asset.go:127-169` | `CraftMaterial` 不检查背包上限 |

---

## 三、🟡 Cosmos SDK v2 桩代码 (12项，有意延期)

> 以下文件带有 `//go:build cosmos` 构建标签，不参与当前编译。
> 属于 Cosmos SDK 全栈迁移的远期规划路径，非紧急。

| # | 文件:行 | 缺失内容 |
|:---|:---|:---|
| V01 | `app/app_v2.go:157-158` | AnteHandler 被注释（签名验证、双密钥检查） |
| V02 | `app/app_v2.go:208-211` | `GetSubspace` 函数体为空 |
| V03 | `app/app_v2.go:240-244` | `MakeTxConfig` 返回 nil |
| V04 | `app/app_v2.go:258-276` | `ExportAppStateAndValidators` 导出空 genesis |
| V05 | `x/brightchain/keeper/keeper_v2.go:123` | RegisterUser 缺失 ZK 身份证明 |
| V06 | `x/brightchain/keeper/keeper_v2.go:322` | `processMonthlySalary` 为空 |
| V07 | `x/brightchain/keeper/keeper_v2.go:326` | `checkOverdueBonds` 为空 |
| V08 | `x/darkchain/keeper/keeper_v2.go:80-82` | RequestLoan 缺失 ZK/双花/身份标记验证 |
| V09 | `x/darkchain/keeper/keeper_v2.go:219` | `checkOverdueLoans` 为空 |
| V10 | `x/brightchain/keeper/msg_server.go:48-49` | Repay 缺失 ZK + 跨链结算 |
| V11 | `x/darkchain/keeper/msg_server.go:54` | SettleLoan 缺失 ZK 还款证明 |
| V12 | `x/brightchain/module/module.go:66,92` | gRPC-Gateway + 不变检查为空 |

---

## 四、🟢 已知设计限制 (4项，不修复)

| # | 文件 | 说明 |
|:---|:---|:---|
| L01 | `x/brightchain/types/types.go:35-41` | 空桩函数是非 Cosmos 构建的正当设计，已有注释 |
| L02 | `frame/types.go:69` | StreamID 始终为 0（QUIC 未实现，TCP 不需要） |
| L03 | `node/anon_integration.go` | 匿名消息无 ZK 剥离（洋葱路由层提供匿名性） |
| L04 | `onion/selector.go` | `Selector` 与 `path.go` 的 `PathSelector` 重复（低优先重构） |

---

## 五、修复计划

### Phase 1: 关键缺陷 (今天，6项)

| 优先级 | 项目 | 工作量 | 说明 |
|:---|:---|:---|:---|
| 🔴 P0 | R01 nil context → panic | 0.5h | `app/chain.go` 添加 nil 检查或移除 nil 传递 |
| 🔴 P0 | R02 vrg/store 静默错误 | 0.5h | `store.go:47` 传播 error 并添加日志 |
| 🔴 P0 | R03 LoadGenesisTree panic | 0.3h | `store.go:85` nil map 防御检查 |
| 🔴 P0 | Z01-Z03 ZK 硬编码零值 | 2h | `verifier.go` 从链上状态树/Epoch 获取真实值 |
| 🔴 P0 | D01 收据 ID 不确定性 | 0.3h | `shadow_receipt.go:244` 替换 UnixNano 为确定性 seed |
| 🔴 P0 | D02 CollisionEvent 不完整 | 0.3h | `epoch.go:224` 填充 SourceCommunityB |

### Phase 2: 网络与数据修复 (今天，4项)

| 优先级 | 项目 | 工作量 | 说明 |
|:---|:---|:---|:---|
| 🟠 P1 | N01 AddCoreFriend IPv6 | 1h | `topology.go` 从邀请凭证或信标中获取 IPv6/Port |
| 🟠 P1 | N02 beacon sender IPv6 | 0.5h | `node.go:buildBeaconPacket` 填充 SenderIPv6 |
| 🟠 P1 | A01-A02 API 硬编码 | 1h | `grpc_server.go` 从链上查询实际 height/pool balance |
| 🟠 P1 | D03 在线率虚假 | 0.5h | `app.go:recordNodePerformance` 基于实际连接检测 |

### Phase 3: 游戏层修复 (明天，6项)

| 优先级 | 项目 | 工作量 | 说明 |
|:---|:---|:---|:---|
| 🟠 P1 | G03 TransferLeadership | 1h | `guild.go` 实现会长转让 |
| 🟠 P1 | G04 GuildSkill 操作 | 1.5h | `guild.go` 实现 AddSkill/LevelUpSkill/ApplySkill |
| 🟠 P1 | G05 Activity 创建 | 1h | `state/machine.go` 实现 CreateActivity/JoinActivity |
| 🟠 P1 | G06 Market 操作 | 1.5h | `state/machine.go` 实现 ListItem/BuyItem/CancelListing |
| 🟠 P1 | G07 掉落确定性 | 1h | `loot/algorithm.go` 移除 crypto/rand，纯确定性 seed |
| 🟠 P1 | G08 ProcessExpiredChanges | 0.5h | `author/governance.go` 实际应用参数到 economy |

### Phase 4: 游戏层完善 (明天，4项)

| 优先级 | 项目 | 工作量 | 说明 |
|:---|:---|:---|:---|
| 🟡 P2 | G01 测试覆盖 | 3h | `app/game/` 全部包添加基础单元测试 |
| 🟡 P2 | G02 应用集成 | 2h | `app/game/` 连接到 `app.go`，替换 brighttypes.GameInfo |
| 🟡 P2 | G09-G10 返回值修复 | 0.5h | `AddXP` 返回 error，`GetPartyKillContributions` 不改原数据 |
| 🟡 P2 | G11-G14 验证补全 | 1.5h | 公会名称/背包上限/邀请流程/资产验证 |

### Phase 5: 低优先级 (远期，4项)

| 优先级 | 项目 | 工作量 | 说明 |
|:---|:---|:---|:---|
| 🟢 P3 | N03 低阶点检查 | 1h | `noise/handshake.go` 补充 4/8 阶点 |
| 🟢 P3 | C01 ProposeBlock nil | 1h | `consensus.go` 从 mempool 组装实际区块数据 |
| 🟢 P3 | L04 洋葱选择器去重 | 1h | 合并 `selector.go` 和 `path.go` |
| 🟢 P3 | Cosmos v2 12项 | 40h+ | 远期 Cosmos SDK 全栈迁移 |

---

## 六、进度追踪

```
Phase 1 (关键缺陷):  ██████████ 100%  6/6 ✅
Phase 2 (网络/数据): ██████████ 100%  4/4 ✅
Phase 3 (游戏层):    ██████████ 100%  6/6 ✅
Phase 4 (完善):      ██████████ 100%  4/4 ✅
Phase 5 (远期):      ██████████ 100%  3/3 ✅ (C01+L04+N03, V系列Cosmos延期)
─────────────────────────────────
总计:                ██████████ 100%  23/23 ✅
```

### 修复记录

| Phase | 项目 | 修复文件 |
|:---|:---|:---|
| P1 | R01 nil context | `app/chain.go`, `app/app.go` — struct{}{} 替代 nil |
| P1 | R02 store 静默错误 | `x/vrg/store.go` — error 传播 |
| P1 | R03 nil map panic | `x/vrg/store.go` — 已有防御，确认安全 |
| P1 | Z01-Z03 ZK 零值 | `x/zk/verifier.go` — 参数化 + 6 调用方更新 |
| P1 | D01 收据 ID | `x/vrg/shadow_receipt.go` — UnixNano → 确定性 SHA256 |
| P1 | D02 CollisionEvent | `x/vrg/epoch.go` — receiptBinding struct + SourceCommunityB |
| P2 | N01 IPv6/Port | `node/topology.go` — 参数 + 8 调用方更新 |
| P2 | N02 beacon IPv6 | `node/node.go` — getOurIPv6() 填充 |
| P2 | A01-A02 API | `localapi/grpc_server.go` — SetCurrentHeight + PoolBalance |
| P2 | D03 在线率 | `app/app.go` — SetOnlineChecker 回调 |
| P3 | G03 TransferLeadership | `social/guild.go` — 完整实现 + JoinGuild/PromoteMember 修复 |
| P3 | G04 GuildSkill | `social/guild.go` — AddSkill/LevelUpSkill/GetSkills |
| P3 | G05 Activity | `state/machine.go` — Create/Join/Complete |
| P3 | G06 Market | `state/machine.go` — List/Buy/Cancel + findMarketListing |
| P3 | G07 掉落确定性 | `loot/algorithm.go` — crypto/rand → 纯 FNV 确定性 |
| P3 | G08 参数应用 | `author/governance.go` — ProcessExpiredChanges 实际调用 UpdateParams |
| P4 | G02 应用集成 | `app/app.go` — gameEngines map + GetGameEngine + RecordGameTx 转发 |
| P4 | G09 AddXP | `social/guild.go` — 返回 error |
| P4 | G10 非破坏读 | `social/party.go` — copy map + ResetKillContributions |
| P4 | G11-G14 验证 | `asset.go` Name/Type/Metadata/Inventory + `guild.go` 名称 + `party.go` 邀请 |
| P4 | G01 测试 | 4 个新测试文件 (state/social/loot) |
| P5 | N03 低阶点 | `noise/handshake.go` — 2→8 个 RFC 7748 低阶点 |
| P5 | C01 空区块 | `consensus/consensus.go` — FlattenTxs + ComputeBlockHash; pbft.go 去重 |
| P5 | L04 洋葱去重 | `selector.go` 删除，path.go 保留 PathSelector |
